# src/components

Shared, composable UI primitives (Tailwind + shadcn/ui-style — see
`docs/ARCHITECTURE.md`). No business logic here — components render
props/state passed in, they don't fetch or decide.

Empty — Phase 0 scaffold intentionally has no product UI yet. First
real components land alongside Phase 1's application shell.
