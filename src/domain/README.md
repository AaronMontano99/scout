# src/domain

Layer 3 — Sales Profile, Account Brain, Institutional Memory, People
Intelligence, Research Findings, Signals, Account Scores,
Recommendations. See `docs/ARCHITECTURE.md`. Framework-agnostic
business logic, unit-testable without Next.js or a live database.
Must never import Layer 1 (presentation) or a vendor SDK directly.

Empty — first real domain code lands in Phase 2 (Sales Profile +
Import). See `docs/ROADMAP.md`.
