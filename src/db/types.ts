/**
 * Placeholder for Supabase-generated database types
 * (`supabase gen types typescript`). Regenerate once the migrations in
 * supabase/migrations/ are applied to a real project, and replace this
 * file's contents wholesale — do not hand-maintain drift against the
 * schema. See docs/DATA_MODEL.md for the authoritative entity design
 * this will eventually be generated from.
 */
export type Database = Record<string, never>;
