# Copy to .env.local and fill in real values. Never commit .env.local —
# see docs/SECURITY.md. Variable NAMES only below; no real secrets.

# --- Supabase (Postgres + Auth + Storage) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# --- AI provider — model names are config, never hardcoded in business
# logic. See docs/AI_ARCHITECTURE.md. ---
AI_MODEL_EXTRACTION=
AI_MODEL_CLASSIFICATION=
AI_MODEL_REASONING=
AI_MODEL_SUMMARY=
AI_MODEL_EMBEDDING=
ANTHROPIC_API_KEY=

# --- Background jobs — see docs/JOBS_ARCHITECTURE.md ---
TRIGGER_API_KEY=

# --- Billing — architected for, not wired in Phase 0. See docs/COST_MODEL.md ---
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
