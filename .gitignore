# Dependencies
node_modules/

# Next.js
.next/
out/
next-env.d.ts

# Build
dist/
build/
*.tsbuildinfo

# Env — never commit real secrets, see docs/SECURITY.md
.env
.env.*.local
.env.local
!.env.example

# Local SQLite database (see src/db/client.ts, docs/LOCAL_MODE.md)
# Anchored to the repo root — an unanchored `data/` pattern also matches
# src/data/, which is real source code (the entire Local Mode accessor
# layer), not generated data. That bug meant src/data/ was never
# actually committed; see the fix that added it in the same change as
# this comment.
/data/

# Supabase local dev (historical reference only — see supabase/README.md)
supabase/.branches/
supabase/.temp/

# Testing
coverage/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Logs
*.log
npm-debug.log*

# Trigger.dev
.trigger/
